<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('../layout/head')%>
    <title>Home</title>
</head>
<body>
    <%-include('../layout/header')%>
    <main id="home-main">
        <section>
            <a href="/searches/new">Start</a>
            <br>
            <h3>Your Book Collection</h3>
            <br>
            <h4><%=result.length%> Book</h4>
            <br>
            <% result.forEach(book=>{%>
                <p><%= book.title %></p>
                <br>
                <img src="<%= book.image_url %>">
                <a href="/books/<%=book.id%>">view details</a>

            <%}) %>
        </section>
    </main>
</body>
</html>